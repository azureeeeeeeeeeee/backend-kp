<p>Gunakan tautan berikut untuk mengatur ulang kata sandi Anda:</p>
<p>
    <a href="{{ $baseURL }}/reset-password?token={{ $content }}">
        {{ $baseURL }}/reset-password?token={{ $content }}
    </a>
</p>